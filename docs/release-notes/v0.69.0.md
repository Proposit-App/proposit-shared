# Release notes — upcoming

Usernames reserved for Proposit itself are now refused. Anything containing
"proposit" is reserved for internal and staff accounts, and the name "admin" is
reserved outright — though names that merely contain "admin", like
`administrator_fan`, are unaffected. Usernames already in use are not changed.
