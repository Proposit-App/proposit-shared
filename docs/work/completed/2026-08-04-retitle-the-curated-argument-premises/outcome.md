# Outcome: Retitle the curated argument premises

Branch `work/curated-premise-retitle`, commit `20ff225`.

## What changed

All 28 premise titles across the four curated fixtures replaced with noun
phrases naming the inferential move.

| Fixture | Premises | Example |
|---|---|---|
| `socrates-01.argument.yaml` | 5 | `Socrates must not escape` → `The case against escape` |
| `madison-01.argument.yaml` | 11 | `If "Self-interest biases judgment" and …` → `Interested judges of their own cause` |
| `mill-01.argument.yaml` | 6 | `If "Suppression assumes infallibility" and …` → `The argument from fallibility` |
| `singer-01.argument.yaml` | 6 | `If "Luxury spending can save lives" and …` → `Putting a price on the rescue` |

Every title is a noun phrase, none paraphrases its consequent claim, all are
under 60 characters with no trailing punctuation. Curly apostrophes match the
surrounding claim bodies.

## Diff constraint held

Verified by inspection rather than by assertion: no `symbol`, `body`, `type`,
`operator`, `children`, `role`, `description`, `documentCurationId`, or
`provenance` line appears in the fixture diff. Only premise `title:` values
moved, and the line-count change (`+28 / -73`) is the old multi-line folded
titles collapsing to single lines.

## One file the request did not anticipate

`src/fixtures/historical-figures/content.generated.ts` is a **committed**
build artifact that embeds the fixture YAML, regenerated by `gen:fixtures`
inside `pnpm run build`. Its diff is 28 insertions / 28 deletions — the same
title strings — and it is included in the commit. Leaving it stale would make
the next person's build produce an unexplained diff.

## Verification

`pnpm run check` passes: 118 test files, 1158 tests, prettier and eslint clean,
build clean. Re-run independently by the coordinating session, not taken on
report. Working tree clean afterwards.

## Not done here, deliberately

- **Version bump.** Left to closeout, per the request's shared-minor call.
- **`.claude/skills/add-curated-argument/SKILL.md`** — workspace-root owned, not
  in this repo. Still open at the epic level.
