# Capability wording: switch to the draft version from a published argument

Tags: `product`

## Product changes

Declare a master capability for jumping from a published argument you own
straight to its working draft, without going through version history.

Requested by the mobile item
`proposit-mobile/2026-07-22-switch-to-the-draft-version-from-a-published-argument`,
which cannot complete its capabilities gate until this master entry exists.

## Technical changes

Ledger-only. One entry under `docs/capabilities/arguments/`, declared here
because the concept is cross-platform — the web app has the same
published/draft split — and this repo owns the master that server and mobile
federate from. Consumers realize it as a status override, not a standalone
declaration.

Status here is `Missing`: shared declares the wording, consumers declare
whether they have built it.

## Meta changes

None.
