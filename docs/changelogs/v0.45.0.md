# Changelog — v0.45.0

<changes starting-hash="8523b59" ending-hash="66e2195">

## Added

- `@proposit/shared/api-client`: argument-participant methods registered on the
  factory — `getArgumentParticipants`, `addArgumentEditor`,
  `removeArgumentParticipant` (in `api-client/argument/participants.ts`), plus the
  `AddParticipantRequestSchema` / `TAddParticipantRequest` request body in
  `schemas/api/argument`.
- `@proposit/shared/api-client`: registration/invitation methods registered on the
  factory — `activateRegistrationInvite`
  (`api-client/user/activate-registration-invite.ts`) and `issueInvitation`
  (`api-client/user/issue-invitation.ts`).

## Changed

- Repinned the `@proposit/proposit-core` dev dependency from a local tarball to the
  registry range `^3.0.0` (the `^3.0.0` peer requirement is unchanged).

</changes>
