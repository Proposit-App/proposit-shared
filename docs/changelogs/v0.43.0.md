# Changelog — v0.43.0

## Added

- New sub-path `@proposit/shared/engine/skeleton-inference`: pure operator-
  inference over `TProjectReactiveSnapshot` for the skeleton-building authoring
  UX. Exports `computeWrap`, `defaultSkeletonOperator`, `rootNegationUnitId`,
  `planSkeletonCommit`, and the types `TSkeletonOperator`,
  `TSkeletonCommitTarget`, `TSkeletonCommitPlan`. Lifted verbatim from the
  server's local copy so server and mobile can share one implementation
  (server consumes it and drops its copy in a follow-up). Purely additive.
