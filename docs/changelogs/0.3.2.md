# Upcoming changelog

- fix(api-client): pass absolute URL (origin + path + query) to `fetchImpl` in `getAllArguments`, `getUserSources`, `getUserClaims`, and `getExpressions`. Previously path-only, which broke React Native and Node consumers.
